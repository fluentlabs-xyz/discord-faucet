services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: fluent-faucet-bot:latest
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
      DB_PATH: /data/faucet.sqlite
    volumes:
      - ./faucet-data:/data
